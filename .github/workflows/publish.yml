name: publish

on:
  push:
    branches:
      - main
      - develop

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: true
        
      - name: Install Dependencies 🔧
        run: npm install

      - name: Build Env Variables ✂️
        run: |
          echo TOKEN_GITHUB=${{ secrets.TOKEN_GITHUB }} >> .env
          npm run build-env-variables

      - name: Build 🏗️
        run: npm run build --output-path=dist-tmp

      - name: Publish 📨
        env:
          SRC_FOLDER_PATH: 'dist-tmp'
          TARGET_BRANCH: 'debug'
        run: |
          # https://stackoverflow.com/a/69578322/8097218
          files=$(find $SRC_FOLDER_PATH -type f)
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git fetch
          git checkout $TARGET_BRANCH
          echo "------------------------------------"
          echo $files
          echo "------------------------------------"
          git checkout ${GITHUB_REF##*/} -- $files # copy files from the source branch
          git add .
          git diff-index --quiet HEAD ||  git commit -am "publish"
          git push origin $TARGET_BRANCH --force
